/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html", // if this file exists
    "./src/**/*.{js,ts,jsx,tsx}", // Ensure you are pointing to all JS, TS, JSX, TSX files in the 'src' directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
